import { NumeroDirective } from './numero.directive';

describe('NuumerDirective', () => {
  it('should create an instance', () => {
    const directive = new NumeroDirective();
    expect(directive).toBeTruthy();
  });
});
